# Infraestructura

La infraestructura principal está declarada en `../template.yaml` usando AWS SAM.

Se mantiene este directorio para futuras extensiones del proyecto (parámetros por ambiente, diagramas generados o migración a Terraform), conservando una estructura de repositorio clara sin duplicar la fuente de verdad actual.
